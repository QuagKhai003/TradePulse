# Progress — 2026-07

Newest at top. One entry per unit of work — features, fixes, bugs, data notes, decisions.
(One file per month: `YYYY-MM.md`.)

---

## 2026-07-10 — Batch 1.6: Layer-2 buyer/seller profiles (plan §7.4)
Branch `phase/1-profiles`.

- **`content/companies/pellets.json`** — 10 curated profiles (6 sellers, 4 buyers), SAMPLE-labelled,
  each with an evidence source (FSC/SBP/PEFC/tender/VIFOREST) + verified date. **No contact fields.**
- **`lib/companies.js`** + **`/profiles`** + **`CompanyCard`** — SSR directory grouped by role; free
  tier shows 3 full, rest blurred with an upgrade mask (plan §11). Home links to it.
- **Golden Rule check** — smoke test confirms no email/phone/@domain renders. Inform, never match.
- **Honesty** — profiles are placeholders; real curation is manual (see S-001 accuracy bar in BUGS).

## 2026-07-10 — Batch 1.5: country drill-down + sourcing chart (plan §7.3)
Branch `phase/1-drilldown`.

- **ETL** — fixture regenerated with JP/KR partner breakdowns (VN, Indonesia, Malaysia, Canada, US /
  Russia); `pipeline` now pulls ALL partners (was World+VN); `config.COUNTRY_NAMES` added.
  `export.py` adds per-market `partners` (share + YoY, importer-reported) + `sourcing` series.
- **Web** — `/market/[slug]` route: `PartnerTable` (Vietnam row highlighted ★) + SSR stacked-bar
  `SourcingChart`; market tiles now link into the drill-down. Markets without partner data degrade
  to a friendly "no sourcing data" note (EU/US/GB).
- **Verified** — 8 Python tests green (pipeline count 42→84); build clean; JP drill shows partner
  shares (VN 55%) + 30-rect sourcing chart; EU shows the graceful empty state.

## 2026-07-10 — Batch 1.4: product search → HS chip + locked pages
Branch `phase/1-search`. Ties plan §7.2 (search) + §7.6 (locked = telemetry).

- **`lib/catalog.js`** — hand-mapped everyday-words → HS lookup (pellets covered; sawn wood, tea,
  coffee, shrimp, cashew, rice locked). `search()` is diacritic-insensitive ("trà"=="tra", đ→d).
- **`SearchBox.js`** (client) — autocomplete; select → `?hs=<code>` (keeps lang); covered/soon badge.
- **`LockedProduct.js`** (client) — "coming soon — request it" for uncovered HS; logs view + request.
- **`/api/locked-click`** — appends demand telemetry to `data/locked_clicks.ndjson` (gitignored;
  the roadmap oracle. Folded into `locked_page_clicks` table in batch 1.8).
- **`page.js`** — branches covered → map screen vs uncovered → locked page; SAMPLE banner only on covered.
- **Verified** — build clean; pellets shows map, `?hs=090240` (tea) shows locked page, POST logs an entry.
- No new Python tests (web-only batch; logic is the pure `catalog.search`, exercised via UI).

## 2026-07-10 — Batch 1.3: Next.js demand map + feed — LOCALHOST MVP RUNS (skeleton step 3)
Branch `phase/1-map`. Next.js 15 App Router (JS), d3-geo choropleth. `npm install` pure-JS.

- **`web/app/page.js`** — SSR Layer-1 landing: reads the snapshot (seam), renders header (product +
  HS badge + honest period), choropleth, market tiles, signal feed, SAMPLE banner, disclaimer.
- **`components/WorldMap.js`** — d3-geo Natural-Earth choropleth over world-atlas topojson; fills
  covered markets by band colour (reporter→ISO fixups; EU has no polygon → shown in tiles/feed).
- **`components/MarketTile.js` / `SignalFeed.js`** — value + YoY band + VN share; moderate+ feed.
- **`lib/`** — `snapshot` loader (fs, request-time), `format` (band colours/labels — single source),
  `i18n` (VN default, `?lang=en`).
- **Ports** — pinned to **3200** (3000 was occupied by another project).
- **Verified** — `npm run build` clean; served on `localhost:3200`, choropleth colours + tiles +
  feed correct (JP/KR significant, GB moderate, US none, VN share shown). VN + EN both render.
- **Deferred** — export/import toggle (import is the plan default); hover/drill-down → 1.5.

## 2026-07-10 — Batch 1.2: signal compute + web snapshot (walking-skeleton step 2)
Branch `phase/1-signals`. Stdlib-only; deterministic.

- **`signals.py`** — PURE YoY signal compute (plan §6): `classify_band` (minor/moderate/
  significant/surge/collapse), noise floors (value ≥$10M, base ≥$2M, history ≥4q), new-lane
  (present near-zero base). `now_iso` injected (no clock in the logic path — Golden Rule).
- **`export.py`** — builds `web/public/data/snapshot.json` (the web/ETL seam): per-market latest
  value + band + direction + VN share + history, plus a moderate+ feed sorted by severity then
  value. Carries honest labels (period, published_date, is_sample).
- **`db.py`** — `fetch_flows` + `upsert_signals`. **`__main__`** now chains pull→signals→snapshot.
- **Result:** JP significant↑ (+34.6%), KR significant↓ (−53%), EU/GB moderate, US suppressed
  (below $10M floor); feed=4. **Tests:** +6 (8 total) — band edges, both floor rejections, fixture signals. Green.

## 2026-07-10 — Batch 1.1: ETL → trade_flows (walking-skeleton step 1)
Branch `phase/1-etl-comtrade`. First app code. Python 3.14, **stdlib-only (no pip)**.

- **`etl/tradepulse_etl/`** — `config` (pilot scope: pellets HS 440131 × JP/KR/EU/US/UK + signal
  thresholds), `db` (SQLite schema `trade_flows`+`signals`, idempotent upsert = persistence seam),
  `transform` (PURE raw→row), `pipeline` (pull→store raw→transform→upsert), `sources/` (seam:
  `base` + `fixture` offline + `comtrade` live via stdlib urllib).
- **`etl/data/fixtures/pellets.json`** — 42-record SAMPLE raw dataset (labelled, not real figures),
  shaped to exercise every signal band (JP surge, KR decline, EU/GB moderate, US below floor) + a generator.
- **CLI** — `python -m tradepulse_etl [--source fixture|comtrade]` → `data/tradepulse.sqlite` (gitignored).
- **Decision** — web will read an ETL-exported JSON snapshot, not bind SQLite in Node (avoids native
  `better-sqlite3` build on Windows; keeps `npm install` pure-JS). Documented as a seam.
- **Tests:** +2 offline (populate=42 rows, idempotent re-run, raw-persisted-before-transform). Green.
- **Sequence note:** dropped the parallel stage0 track (owner: sequential build). ADR-0001 stays on record.

## 2026-07-10 — ADR-0002: Phase 1 MVP build plan (docs)
Branch `docs/adr-0002-phase1-plan`. No code. Owner elected parallel build+validate.

- **`docs/decisions/0002-phase1-mvp.md`** — batches 1.1–1.9 with acceptance lines; walking
  skeleton = 1.1(ETL)→1.2(signals+test)→1.3(map). Tech settled: SQLite-behind-seam, Next.js
  SSR+API, D3 choropleth, Python ETL raw-before-transform, VN-default i18n. Depth 1.6–1.9 gated on Stage 0 GO.
- **`ROADMAP` / `decisions/README` / `STATUS`** — Phase 1 → IN PROGRESS; ADR-0002 linked.

## 2026-07-10 — Adopt the file-driven workflow kit (docs-only)
Branch `docs/adopt-workflow-kit`. No code (Stage 0). Bootstrapped the repo + living docs.

- **git** — initialized repo (`main`), global identity, no AI attribution in commits.
  Baseline commit = product plan + `.gitignore`.
- **`.gitignore`** — excludes dev/product-build/testing artifacts, secrets (`.env`),
  re-pullable data caches.
- **`CLAUDE.md`** — adapted to TradePulse: what-it-is, Golden Rule ("Inform, never match" +
  deterministic-signals + cite-every-source), stack, run block, current state, working agreement.
- **`docs/`** — `CONVENTIONS` (+build loop §10, seam §11), `STATUS`, `ROADMAP`, `DATA_MODEL`
  (plan §10.2 schema, PLANNED), `BUGS` (L-001 lag, L-002 mirror, S-001 accuracy liability),
  `ONBOARDING`, this changelog.
- **`dev-workflow-kit/`** — removed after adapting; cited loop/seam folded into `CONVENTIONS.md`.
- **`docs/decisions/`** — ADR index + `0001-stage0-validation` (Stage 0 batch plan) + template.
- **Git policy override** — every change on its own branch; **never merge without owner approval**.
- **Tests:** none (no code yet).
